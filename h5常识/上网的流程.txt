在地址栏敲下网址之后，通过公网的dns寻找到公网的dns服务器，通过tcp协议寻找到公司的ip，在找到公司的服务器，再寻找当前的静态请求页面，比如请求一个index.jsp文件，在通过文件关联获取到文件组信息，再通过tcp协议，拉取到本地用户进行数据对传，下载到电脑上，然后由浏览器由上至下的对页面进行渲染（单线程），从dtd开始一直到页面结束，如果碰到JS的script标签和CSS的link标签就会进行分线程的渲染。